import React from 'react';

// ELEMENTS
import { Grid, FixedBox } from '../elements';

const Confirm = ({ children }) => {
  return <FixedBox></FixedBox>;
};

Confirm.propTypes = {};

export default Confirm;
